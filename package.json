{
  "name": "airflow-tool",
  "private": true,
  "description": "本地全栈开发：npm run dev 同时起 FastAPI + Vite；仅前端用 npm run dev:web",
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:web": "npm run dev --prefix frontend",
    "dev:api": ".venv-backend/bin/uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
